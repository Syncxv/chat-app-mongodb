// import { NextPage } from "next";
// import { createContext } from "vm";

// export const AuthContext = createContext({acessToken: localStorage.getItem('token') || null, user: null});

// const AuthContext : NextPage = ({children}) => {

// }
//ill do it later okay maybe
export const bruh = ':|'
